/**
 * Common utility types shared across domains.
 * Add game-wide enums/constants here as needed.
 */

/** Category type numeric IDs as used in Unit.CategoryType */
export enum CategoryType {
  Logistics = 0,
  Recon = 1,
  Infantry = 2,
  Combat = 3,
  Support = 4,
  Helicopters = 5,
  Air = 6,
}
