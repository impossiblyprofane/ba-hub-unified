# Shared TypeScript Types

This directory contains type definitions shared between frontend and backend.

## Structure

- `types.ts` - Core type definitions (Unit, Deck, Map, PlayerStats, etc.)
- `schemas/` - GraphQL schema types
- `utils/` - Shared utility functions

## Legacy Types

Place your existing TypeScript definitions from the old project in:
- `legacy/` - Original type definitions to migrate from
